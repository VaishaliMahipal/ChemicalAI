import { isAnyArray } from '/node_modules/is-any-array/src/index.js';

import WrapperMatrix1D from '/node_modules/ml-matrix/src/wrap/WrapperMatrix1D.js';
import WrapperMatrix2D from '/node_modules/ml-matrix/src/wrap/WrapperMatrix2D.js';

export function wrap(array, options) {
  if (isAnyArray(array)) {
    if (array[0] && isAnyArray(array[0])) {
      return new WrapperMatrix2D(array);
    } else {
      return new WrapperMatrix1D(array, options);
    }
  } else {
    throw new Error('the argument is not an array');
  }
}
