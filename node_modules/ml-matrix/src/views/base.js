import { AbstractMatrix } from '/node_modules/ml-matrix/src/matrix.js';

export default class BaseView extends AbstractMatrix {
  constructor(matrix, rows, columns) {
    super();
    this.matrix = matrix;
    this.rows = rows;
    this.columns = columns;
  }
}
